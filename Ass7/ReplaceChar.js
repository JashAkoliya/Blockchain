// 4. Input a String S with a word, and replace character “a” with “x” and
// display the result -
// Input: “apple” Output: “xpple”

let s = "applea";
let res = s.replace(/a/g,"x");
console.log(res);